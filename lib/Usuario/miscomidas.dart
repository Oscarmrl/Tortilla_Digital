import 'package:flutter/material.dart';

class Miscomidas extends StatelessWidget {
  const Miscomidas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mis Comidas')),
      body: const Center(child: Text('Pantalla de Mis Comidas oo')),
    );
  }
}
